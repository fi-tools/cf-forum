require "test_helper"

class TagDeclsControllerTest < ActionDispatch::IntegrationTest
  test "users can set tag_decls for some stuff" do
    skip "use case: declare blog index, link blog index to posts, set post private (only at creation)?"
  end
end
