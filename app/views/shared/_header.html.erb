<section class="hero is-primary is-small">
  <!-- Hero head: will stick at the top -->
  <div class="hero-head">
    <nav class="navbar">
      <div class="container">
        <div class="navbar-brand">
          <%= link_to root_path, class: "navbar-item" do %>
            <span class="has-text-weight-bold is-size-4 ml-4">CF Forum</span>
          <% end %>
          <span class="navbar-burger" data-target="navbarMenuHeroA">
            <span></span>
            <span></span>
            <span></span>
          </span>
        </div>
        <div id="navbarMenuHeroA" class="navbar-menu">
          <div class="navbar-end">
            <%# <a class="navbar-item is-active">
              Home
            </a>
            <a class="navbar-item">
              Examples
            </a>
            <a class="navbar-item">
              Documentation
            </a>
            <span class="navbar-item">
              <a class="button is-primary is-inverted">
                <span class="icon">
                  <i class="fab fa-github"></i>
                </span>
                <span>Download</span>
              </a>
            </span> %>
            <!--
            <span class="navbar-item">
              <%= link_to new_node_path, class: "button is-success" do %>
                <span>New Post</span>
              <% end %>
            </span>
              -->
            <% if !current_user %>
              <span class="navbar-item">
                <%= link_to 'New Acct', new_user_registration_path, class: "button is-warning" %>
              </span>
              <span class="navbar-item">
                <%= link_to 'Sign In', new_user_session_path, class: "button is-warning" %>
              </span>
            <% else %>
              <span class="navbar-item">
                <%= link_to 'My Identities', edit_user_authors_path, class: "button is-info" %>
              </span>
              <span class="navbar-item">
                <%= link_to 'My Account', edit_user_registration_path, class: "button is-info" %>
              </span>
              <span class="navbar-item">
                <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "button is-warning" %>
              </span>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
  </div>
  <!-- Hero content: will be in the middle -->
  <div class="hero-body">
    <div class="container has-text-centered">
      <h1 class="title">
        <% if content_for(:title) %>
          <%= yield :title %>
        <% else %>
          CF Forum (default)
        <% end %>
      </h1>
      <% if content_for(:subtitle) %>
        <h2 class="subtitle">
          <%= yield :subtitle %>
        </h2>
      <% end %>
    </div>
  </div>
  <!-- Hero footer: will stick at the bottom -->
  <%# <div class="hero-foot">
    <nav class="tabs">
      <div class="container">
        <ul>
          <li class="is-active"><a>Overview</a></li>
          <li><a>Modifiers</a></li>
          <li><a>Grid</a></li>
          <li><a>Elements</a></li>
          <li><a>Components</a></li>
          <li><a>Layout</a></li>
        </ul>
      </div>
    </nav>
  </div> %>
</section>
